import 'package:flutter/material.dart';

class AdminClientsWidget extends StatelessWidget {
  const AdminClientsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Column(
      children: const [
      ],
    ));
  }
}
